language: ruby
rvm:
  - 2.1.2
notifications:
  email:
    on_success: always
    on_fail: always
before_script:
  - cp config/database.yml.travis config/database.yml
  - psql -c 'create database givepuppies_test;' -U postgres
script:
  - rake spec:travis